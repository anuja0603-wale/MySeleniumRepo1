package HelperMethod;
import org.openqa.selenium.chrome.ChromeDriver;

import openga.selenium.WebDriver;
import 
public class HelperMethod {
	public void  demo()
	{
		WebDriver driver=new ChromeDriver();
		driver.manage().window().
	}
}
